{
  "dependencies": {
    "gsap": "^3.13.0"
  },
  "scripts": {
    "build": "bun build ./index.html --minify --outdir=dist",
    "dev": "bun run ./index.html --hot",
    "deploy": "bun run build && git subtree push --prefix dist origin gh-pages"
  }
}
